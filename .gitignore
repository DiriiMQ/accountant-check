# Real business/financial data - never commit to this public repo
*.xlsx
*.xls
~$*
final.zip
final/

# OS / editor junk
.DS_Store
__pycache__/
*.pyc

# PyInstaller build output (built by CI, not committed)
build/
dist/
*.spec

# Rust build output (built by CI, not committed)
rust/target/
